apiVersion: apps/v1
kind: Deployment
metadata:
  name: locust-worker
  namespace: load-test
  labels:
    app: locust-worker
spec:
  replicas: 8
  selector:
    matchLabels:
      app: locust-worker
  template:
    metadata:
      labels:
        app: locust-worker
    spec:
      containers:
        - name: locust-worker
          image: ghcr.io/getsentry/ingest-load-tester:latest
          args: ["run-worker"]
          env:
            - name: LOCUST_FILE
              value: http_locustfile.py
            - name: MASTER_HOST
              value: locust-master
            - name: WORKER_PROCESSES
              value: "6"
            - name: SENTRY_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: load-tester-secrets
                  key: sentry-auth-token
          volumeMounts:
            - name: config
              mountPath: /app/config
          resources:
            requests:
              cpu: "6"
              memory: "4Gi"
            limits:
              memory: "4Gi"
      volumes:
        - name: config
          configMap:
            name: locust-config

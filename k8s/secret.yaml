# IMPORTANT: Replace placeholder values before deploying.
# You can also create this secret imperatively:
#   kubectl create secret generic load-tester-secrets -n load-test \
#     --from-literal=sentry-auth-token='<your-sentry-token>'
apiVersion: v1
kind: Secret
metadata:
  name: load-tester-secrets
  namespace: load-test
type: Opaque
stringData:
  sentry-auth-token: "REPLACE_ME"
